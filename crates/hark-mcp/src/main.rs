//! Placeholder for the stdio MCP server (rmcp), which lands in Phase 4.
//! It will open the Hark database read-only and expose search/transcript tools.

fn main() {
    eprintln!("hark-mcp: not yet implemented (Phase 4)");
    std::process::exit(1);
}
